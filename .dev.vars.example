# Cloudflare Access OIDC Application Credentials
# Get these from: Cloudflare One Dashboard → Access → Applications → Your SaaS App
ACCESS_CLIENT_ID=<Copy from "Client ID" in dash>
ACCESS_CLIENT_SECRET=<Copy from "Client secret" in dash>
ACCESS_TOKEN_URL=<Copy from "Token endpoint" in dash>
ACCESS_AUTHORIZATION_URL=<Copy from "Authorization endpoint" in dash>
ACCESS_JWKS_URL=<Copy from "Key endpoint" in dash>

# Cookie encryption key - generate with: openssl rand -hex 32
COOKIE_ENCRYPTION_KEY=<generate using openssl rand -hex 32>

# SiYuan kernel URL - the SiYuan Note server to connect to
# Example: https://siyuan.example.com or http://localhost:6806 for local development
SIYUAN_KERNEL_URL=http://localhost:6806

# SiYuan API token (optional, only if authentication is enabled on kernel)
# SIYUAN_KERNEL_TOKEN=your-siyuan-token

# RAG backend configuration (optional, for vector search)
# RAG_BASE_URL=http://127.0.0.1:26808
# RAG_API_KEY=your-rag-api-key
